<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="ring">
            <corners android:radius="5dp" />
            <gradient
                android:angle="270"
                android:centerColor="@color/color_tertiary"
                android:endColor="@color/color_primary"
                android:startColor="@color/color_secondary" /> <!-- Background color -->
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="line">
                <corners android:radius="5dp" />
                <gradient
                    android:angle="270"
                    android:centerColor="@color/color_tertiary"
                    android:endColor="@color/color_primary"
                    android:startColor="@color/color_secondary" /> <!-- Gradient from top to bottom -->
            </shape>
        </clip>
    </item>
</layer-list>
